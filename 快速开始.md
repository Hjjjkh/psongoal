# 快速开始指南

## 🚀 立即开始

### 1. 安装 pnpm（如果未安装）

```bash
npm install -g pnpm
```

### 2. 安装依赖

```bash
# 从项目根目录运行
pnpm install
```

### 3. 配置环境变量

在 `packages/web/` 目录下创建 `.env.local` 文件：

```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
```

**获取 Supabase 配置**：
1. 登录 [Supabase Dashboard](https://app.supabase.com)
2. 选择你的项目
3. 进入 **Settings** > **API**
4. 复制 **Project URL** 和 **anon public** key

### 4. 启动开发服务器

```bash
# 从根目录运行（推荐）
pnpm dev:web

# 或从 packages/web 目录运行
cd packages/web
pnpm dev
```

### 5. 访问应用

打开浏览器访问：`http://localhost:3000`

---

## 📦 项目结构

```
psongoal/
├── packages/
│   ├── web/          # 网页版（当前使用）
│   ├── core/         # 共享逻辑（待开发）
│   ├── mobile/       # App（待开发）
│   ├── desktop/      # exe（待开发）
│   └── miniprogram/  # 小程序（待开发）
├── package.json      # 根配置
└── pnpm-workspace.yaml
```

---

## 🔧 常用命令

### 开发

```bash
# 启动网页版开发服务器
pnpm dev:web

# 或
pnpm dev
```

### 构建

```bash
# 构建网页版
pnpm build:web

# 或
pnpm build
```

### 生产运行

```bash
# 启动生产服务器
pnpm start
```

---

## 🚀 Zeabur 部署

### 快速配置

1. **连接 GitHub 仓库**
   - 在 Zeabur Dashboard 中导入你的 GitHub 仓库

2. **设置根目录**
   - 在项目设置中，设置 **Root Directory** 为：`packages/web`

3. **配置环境变量**
   - 添加以下环境变量：
     ```
     NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
     NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
     ```

4. **配置 pnpm**
   - 添加环境变量：`NPM_CONFIG_PACKAGE_MANAGER=pnpm`

5. **部署**
   - 点击 **Deploy** 按钮

### 详细步骤

请参考：`ZEABUR_DEPLOYMENT_CONFIG.md`

---

## 📚 相关文档

- **环境配置**: `环境配置说明.md`
- **Zeabur 部署**: `ZEABUR_DEPLOYMENT_CONFIG.md`
- **Monorepo 设置**: `MONOREPO_SETUP_COMPLETE.md`
- **多平台开发**: `多平台开发待办清单.md`

---

## ❓ 常见问题

### 1. 依赖安装失败

**解决**：
```bash
# 清除缓存
pnpm store prune

# 重新安装
pnpm install
```

### 2. 环境变量未生效

**解决**：
1. 确认 `.env.local` 文件在 `packages/web/` 目录下
2. 确认变量名以 `NEXT_PUBLIC_` 开头
3. 重启开发服务器

### 3. Zeabur 部署失败

**解决**：
1. 确认 Root Directory 设置为 `packages/web`
2. 确认环境变量已配置
3. 查看 Zeabur 构建日志

---

**需要帮助？** 查看详细文档或提交 Issue。

