# 多平台开发待办清单

> 基于《多平台开发部署完整方案.md》创建的实施清单

---

## 📋 总体进度

- [x] 阶段 1: 准备 Monorepo（1周）✅ **已完成**
- [ ] 阶段 2: 开发 App（2-3周）
- [ ] 阶段 3: 开发 exe（2-3周）
- [ ] 阶段 4: 开发小程序（1-2周）

---

## 🏗️ 阶段 1: 准备 Monorepo（1周）

### 1.1 创建 Monorepo 结构

- [x] 安装 pnpm（如果未安装）✅
  ```bash
  npm install -g pnpm
  ```

- [x] 创建目录结构 ✅
  ```bash
  mkdir -p packages/{core,web,mobile,desktop,miniprogram}
  mkdir -p scripts
  ```

- [x] 创建根 `package.json`（workspace 配置）✅
  - [x] 配置 workspaces: `["packages/*"]`
  - [x] 添加脚本命令：
    - [x] `dev:web`
    - [x] `dev:mobile`
    - [x] `dev:desktop`
    - [x] `build:web`
    - [x] `build:mobile`
    - [x] `build:desktop`
    - [x] `build:core`

- [x] 创建 `pnpm-workspace.yaml` ✅
  ```yaml
  packages:
    - 'packages/*'
  ```

### 1.2 迁移现有代码到 packages/web

- [x] 移动现有文件到 `packages/web/` ✅
  - [x] `app/` → `packages/web/app/`
  - [x] `components/` → `packages/web/components/`
  - [x] `lib/` → `packages/web/lib/`
  - [x] `public/` → `packages/web/public/`
  - [x] `supabase/` → `packages/web/supabase/`
  - [x] 配置文件：
    - [x] `next.config.js`
    - [x] `tailwind.config.ts`
    - [x] `tsconfig.json`
    - [x] `.env.local`（保留环境变量）

- [x] 创建 `packages/web/package.json` ✅
  - [x] 复制现有依赖
  - [x] 更新脚本命令
  - [x] 添加 workspace 依赖（如需要）

- [x] 测试网页版是否正常运行 ✅
  ```bash
  cd packages/web
  pnpm install
  pnpm dev
  ```

### 1.3 创建共享逻辑包 (packages/core) - ✅ 基础已完成

**说明**: 共享逻辑包主要为 App/exe 开发准备。网页版已可正常运行，不需要立即迁移。

- [x] 创建 `packages/core/package.json` ✅
  - [x] 设置包名和版本
  - [x] 配置 TypeScript
  - [x] 配置构建脚本

- [x] 创建 `packages/core/tsconfig.json` ✅

- [x] 提取共享业务逻辑（基础部分）✅
  - [x] `src/types.ts` - 类型定义
  - [x] `src/insights.ts` - 智能建议逻辑（纯业务逻辑）
  - [x] `src/utils.ts` - 工具函数（纯业务逻辑）
  - [x] `src/db-interface.ts` - 数据库抽象接口定义

- [x] 构建核心包 ✅
  ```bash
  cd packages/core
  pnpm build
  ```

**待后续（开发 App/exe 时）**:
- [ ] 提取 `system-state.ts` 的业务逻辑（需要抽象数据库操作）
- [ ] 实现完整的数据库适配器（Supabase、SQLite）
- [ ] 网页版迁移到共享逻辑包（可选）

### 1.4 配置网页版使用共享逻辑 - ⏸️ 暂缓（网页版已可正常运行）

**说明**: 网页版已可正常运行，使用 `packages/web/lib/core/` 中的逻辑即可。共享逻辑包的完整提取和迁移可以在开发 App/exe 时进行。

- [x] 测试网页版功能完整性 ✅
  - [x] 创建目标
  - [x] 创建阶段和行动
  - [x] 完成行动
  - [x] 查看复盘
  - [x] 专注空间
  - [x] Zeabur 部署

**待后续（开发 App/exe 时，可选）**:
- [ ] 在 `packages/web/package.json` 中添加 core 依赖
- [ ] 更新 `packages/web/lib/core/` 使用 `@psongoal/core`
- [ ] 创建 `packages/web/lib/db/supabase-adapter.ts`

### 1.5 验证 Monorepo 配置

- [x] 从根目录运行命令 ✅
  ```bash
  pnpm install
  pnpm dev:web
  ```

- [x] 确认 workspace 链接正常 ✅
  ```bash
  pnpm list
  ```

- [x] 更新 `.gitignore`（如果需要）✅

---

## 📱 阶段 2: 开发 App（2-3周）

### 2.1 初始化 React Native 项目

- [ ] 创建 React Native 项目
  ```bash
  cd packages/mobile
  npx react-native init psongoal --template react-native-template-typescript
  ```

- [ ] 配置 `packages/mobile/package.json`
  - [ ] 添加依赖：
    - [ ] `react-native-sqlite-storage` 或 `react-native-sqlite-2`
    - [ ] `@react-navigation/native`
    - [ ] `@react-navigation/stack`
    - [ ] `@psongoal/core`（共享逻辑）

- [ ] 安装依赖
  ```bash
  pnpm install
  ```

### 2.2 实现 SQLite 适配器

- [ ] 创建 `packages/mobile/src/db/sqlite-adapter.ts`
  - [ ] 实现 `DatabaseAdapter` 接口
  - [ ] 初始化 SQLite 数据库
  - [ ] 创建表结构（goals, phases, actions, daily_executions, system_states）
  - [ ] 实现 CRUD 操作

- [ ] 创建数据库迁移脚本
  - [ ] 初始表结构
  - [ ] 索引创建

- [ ] 测试数据库操作
  - [ ] 创建目标
  - [ ] 查询数据
  - [ ] 更新数据
  - [ ] 删除数据

### 2.3 实现 UI 界面

- [ ] 安装 UI 库（可选）
  - [ ] `react-native-paper` 或 `native-base`
  - [ ] 或使用自定义组件

- [ ] 创建屏幕组件
  - [ ] `src/screens/HomeScreen.tsx`（今日行动）
  - [ ] `src/screens/GoalsScreen.tsx`（目标规划）
  - [ ] `src/screens/DashboardScreen.tsx`（复盘看板）
  - [ ] `src/screens/SettingsScreen.tsx`（设置）
  - [ ] `src/screens/FocusScreen.tsx`（专注空间）

- [ ] 创建共享组件
  - [ ] `src/components/Card.tsx`
  - [ ] `src/components/Button.tsx`
  - [ ] `src/components/Input.tsx`
  - [ ] `src/components/Chart.tsx`（使用 `react-native-chart-kit`）

- [ ] 配置导航
  - [ ] 设置 `@react-navigation/native`
  - [ ] 创建导航栈
  - [ ] 配置路由

### 2.4 集成共享逻辑

- [ ] 在 `packages/mobile/package.json` 中添加 core 依赖
  ```json
  "dependencies": {
    "@psongoal/core": "workspace:*"
  }
  ```

- [ ] 使用共享逻辑
  - [ ] 导入 `@psongoal/core` 的类型和函数
  - [ ] 使用 `DatabaseAdapter` 初始化系统状态
  - [ ] 使用 `completeActionAndAdvance` 完成行动
  - [ ] 使用 `generateInsights` 生成建议

- [ ] 测试功能完整性
  - [ ] 创建目标、阶段、行动
  - [ ] 完成行动
  - [ ] 查看复盘数据
  - [ ] 测试所有功能

### 2.5 iOS 配置和测试

- [ ] 配置 iOS 项目
  - [ ] 打开 `ios/psongoal.xcworkspace`
  - [ ] 配置 Bundle Identifier
  - [ ] 配置签名证书（开发/发布）

- [ ] 安装 iOS 依赖
  ```bash
  cd ios
  pod install
  ```

- [ ] 运行 iOS 应用
  ```bash
  pnpm ios
  ```

- [ ] 测试 iOS 功能
  - [ ] 所有功能正常
  - [ ] UI 适配正常
  - [ ] 性能正常

### 2.6 Android 配置和测试

- [ ] 配置 Android 项目
  - [ ] 打开 `android/` 目录
  - [ ] 配置 `build.gradle`
  - [ ] 配置签名（开发/发布）

- [ ] 运行 Android 应用
  ```bash
  pnpm android
  ```

- [ ] 测试 Android 功能
  - [ ] 所有功能正常
  - [ ] UI 适配正常
  - [ ] 性能正常

### 2.7 优化和测试

- [ ] 性能优化
  - [ ] 优化数据库查询
  - [ ] 优化渲染性能
  - [ ] 优化内存使用

- [ ] 测试
  - [ ] 功能测试
  - [ ] UI 测试
  - [ ] 性能测试
  - [ ] 兼容性测试（不同设备）

---

## 💻 阶段 3: 开发 exe（2-3周）

### 3.1 初始化 Electron 项目

- [ ] 创建 Electron 项目
  ```bash
  cd packages/desktop
  mkdir -p src/{renderer,main,db}
  ```

- [ ] 配置 `packages/desktop/package.json`
  - [ ] 添加依赖：
    - [ ] `electron`
    - [ ] `electron-builder`
    - [ ] `better-sqlite3`（SQLite）
    - [ ] `react`
    - [ ] `react-dom`
    - [ ] `@psongoal/core`（共享逻辑）

- [ ] 安装依赖
  ```bash
  pnpm install
  ```

### 3.2 实现 SQLite 适配器

- [ ] 创建 `packages/desktop/src/db/sqlite-adapter.ts`
  - [ ] 实现 `DatabaseAdapter` 接口
  - [ ] 使用 `better-sqlite3` 初始化数据库
  - [ ] 创建表结构
  - [ ] 实现 CRUD 操作

- [ ] 配置数据库路径
  - [ ] Windows: `%APPDATA%/psongoal/database.db`
  - [ ] Mac: `~/Library/Application Support/psongoal/database.db`
  - [ ] Linux: `~/.config/psongoal/database.db`

- [ ] 测试数据库操作
  - [ ] 创建目标
  - [ ] 查询数据
  - [ ] 更新数据
  - [ ] 删除数据

### 3.3 实现 Electron 主进程

- [ ] 创建 `packages/desktop/src/main/main.ts`
  - [ ] 创建窗口
  - [ ] 处理应用生命周期
  - [ ] 配置菜单
  - [ ] 处理 IPC 通信（如果需要）

- [ ] 配置 `packages/desktop/package.json`
  ```json
  "main": "dist/main/main.js"
  ```

- [ ] 配置构建脚本
  ```json
  "scripts": {
    "dev": "electron .",
    "build": "tsc && electron-builder",
    "build:win": "electron-builder --win",
    "build:mac": "electron-builder --mac"
  }
  ```

### 3.4 实现渲染进程（React UI）

- [ ] 创建 React 应用结构
  - [ ] `src/renderer/App.tsx`
  - [ ] `src/renderer/index.tsx`
  - [ ] `src/renderer/index.html`

- [ ] 复用网页版组件（可选）
  - [ ] 复制 `packages/web/components/` 到 `packages/desktop/src/renderer/components/`
  - [ ] 或创建新的组件

- [ ] 配置 Webpack 或 Vite（如果需要）
  - [ ] 构建配置
  - [ ] 开发服务器配置

### 3.5 集成共享逻辑

- [ ] 在 `packages/desktop/package.json` 中添加 core 依赖
  ```json
  "dependencies": {
    "@psongoal/core": "workspace:*"
  }
  ```

- [ ] 使用共享逻辑
  - [ ] 导入 `@psongoal/core` 的类型和函数
  - [ ] 使用 `DatabaseAdapter` 初始化系统状态
  - [ ] 使用 `completeActionAndAdvance` 完成行动
  - [ ] 使用 `generateInsights` 生成建议

- [ ] 测试功能完整性
  - [ ] 创建目标、阶段、行动
  - [ ] 完成行动
  - [ ] 查看复盘数据
  - [ ] 测试所有功能

### 3.6 配置 Electron Builder

- [ ] 创建 `electron-builder.config.js`
  - [ ] Windows 配置
    - [ ] 图标配置
    - [ ] 安装程序配置
    - [ ] 代码签名（可选）
  - [ ] Mac 配置
    - [ ] 图标配置
    - [ ] DMG 配置
    - [ ] 代码签名（可选）

- [ ] 测试构建
  ```bash
  pnpm build:win  # Windows
  pnpm build:mac  # Mac
  ```

- [ ] 测试安装包
  - [ ] 安装测试
  - [ ] 运行测试
  - [ ] 卸载测试

### 3.7 优化和测试

- [ ] 性能优化
  - [ ] 优化数据库查询
  - [ ] 优化渲染性能
  - [ ] 优化内存使用

- [ ] 测试
  - [ ] 功能测试
  - [ ] UI 测试
  - [ ] 性能测试
  - [ ] 兼容性测试（不同系统）

---

## 🎯 阶段 4: 开发小程序（1-2周）

### 4.1 初始化小程序项目

- [ ] 创建小程序项目结构
  ```bash
  cd packages/miniprogram
  mkdir -p {pages,components,utils,app}
  ```

- [ ] 创建 `packages/miniprogram/app.json`
  - [ ] 配置页面路由
  - [ ] 配置窗口样式
  - [ ] 配置 tabBar（如果需要）

- [ ] 创建 `packages/miniprogram/app.js`
  - [ ] 应用生命周期
  - [ ] 全局数据

- [ ] 创建 `packages/miniprogram/project.config.json`
  - [ ] 项目配置
  - [ ] 编译配置

### 4.2 实现 Supabase 适配器

- [ ] 创建 `packages/miniprogram/utils/supabase-adapter.ts`
  - [ ] 实现 `DatabaseAdapter` 接口
  - [ ] 使用 Supabase JS 客户端
  - [ ] 配置认证
  - [ ] 实现 CRUD 操作

- [ ] 配置环境变量
  - [ ] `SUPABASE_URL`
  - [ ] `SUPABASE_ANON_KEY`

- [ ] 测试数据库操作
  - [ ] 创建目标
  - [ ] 查询数据
  - [ ] 更新数据
  - [ ] 删除数据

### 4.3 实现页面

- [ ] 创建页面结构
  - [ ] `pages/home/`（今日行动）
  - [ ] `pages/goals/`（目标规划）
  - [ ] `pages/dashboard/`（复盘看板）
  - [ ] `pages/settings/`（设置）
  - [ ] `pages/focus/`（专注空间）

- [ ] 实现页面逻辑
  - [ ] 数据获取
  - [ ] 用户交互
  - [ ] 状态管理

- [ ] 创建共享组件
  - [ ] `components/card/`
  - [ ] `components/button/`
  - [ ] `components/input/`
  - [ ] `components/chart/`（使用 `ec-canvas` 或 `wx-charts`）

### 4.4 集成共享逻辑

- [ ] 在 `packages/miniprogram/package.json` 中添加 core 依赖（如果支持）
  - [ ] 或直接复制共享逻辑代码（小程序可能不支持 npm）

- [ ] 使用共享逻辑
  - [ ] 导入类型和函数
  - [ ] 使用 `DatabaseAdapter` 初始化系统状态
  - [ ] 使用 `completeActionAndAdvance` 完成行动
  - [ ] 使用 `generateInsights` 生成建议

- [ ] 测试功能完整性
  - [ ] 创建目标、阶段、行动
  - [ ] 完成行动
  - [ ] 查看复盘数据
  - [ ] 测试所有功能

### 4.5 小程序 UI 适配

- [ ] 适配小程序样式
  - [ ] 使用 `rpx` 单位
  - [ ] 适配不同屏幕尺寸
  - [ ] 优化字体大小

- [ ] 优化交互
  - [ ] 触摸反馈
  - [ ] 加载状态
  - [ ] 错误提示

- [ ] 测试 UI
  - [ ] 不同设备测试
  - [ ] 不同屏幕尺寸测试

### 4.6 配置和测试

- [ ] 配置小程序
  - [ ] AppID 配置
  - [ ] 服务器域名配置（Supabase）
  - [ ] 权限配置

- [ ] 使用微信开发者工具测试
  - [ ] 打开项目
  - [ ] 编译运行
  - [ ] 测试所有功能

- [ ] 真机测试
  - [ ] 预览测试
  - [ ] 体验测试

### 4.7 优化和测试

- [ ] 性能优化
  - [ ] 优化数据请求
  - [ ] 优化渲染性能
  - [ ] 优化包体积

- [ ] 测试
  - [ ] 功能测试
  - [ ] UI 测试
  - [ ] 性能测试
  - [ ] 兼容性测试（不同设备）

---

## 🚀 部署准备

### 网页版部署

- [ ] 配置环境变量
  - [ ] `NEXT_PUBLIC_SUPABASE_URL`
  - [ ] `NEXT_PUBLIC_SUPABASE_ANON_KEY`

- [ ] 构建测试
  ```bash
  cd packages/web
  pnpm build
  ```

- [ ] 部署到 Zeabur/Vercel
  - [ ] 连接 GitHub 仓库
  - [ ] 设置根目录为 `packages/web`
  - [ ] 配置环境变量
  - [ ] 自动部署

### App 部署

- [ ] iOS 部署准备
  - [ ] 配置证书
  - [ ] 配置 App Store Connect
  - [ ] 构建发布版本
  - [ ] 上传到 App Store

- [ ] Android 部署准备
  - [ ] 配置签名
  - [ ] 配置 Google Play Console
  - [ ] 构建发布版本
  - [ ] 上传到 Google Play

### exe 部署

- [ ] Windows exe
  - [ ] 构建安装包
  - [ ] 测试安装包
  - [ ] 上传到分发平台

- [ ] Mac app
  - [ ] 构建 DMG/APP
  - [ ] 测试安装包
  - [ ] 上传到分发平台或 App Store

### 小程序部署

- [ ] 上传代码
  - [ ] 在微信开发者工具中上传
  - [ ] 填写版本号和备注

- [ ] 提交审核
  - [ ] 登录微信公众平台
  - [ ] 提交审核
  - [ ] 等待审核通过

- [ ] 发布
  - [ ] 审核通过后发布

---

## 📝 后续优化（可选）

- [ ] CI/CD 配置
  - [ ] GitHub Actions 工作流
  - [ ] 自动构建
  - [ ] 自动部署

- [ ] 文档完善
  - [ ] 开发文档
  - [ ] 部署文档
  - [ ] 用户文档

- [ ] 测试完善
  - [ ] 单元测试
  - [ ] 集成测试
  - [ ] E2E 测试

---

## ✅ 完成检查

- [ ] 所有平台功能完整
- [ ] 所有平台测试通过
- [ ] 所有平台部署成功
- [ ] 文档完善
- [ ] 代码质量检查通过

---

**最后更新**: 2024-12-20  
**预计总时间**: 6-9周

